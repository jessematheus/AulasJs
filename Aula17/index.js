function soma(x, y) {
    const resultado = x + y;
    return resultado;//O motor do JS ao encontrar a palavra RETURN em uma função, nada que for escrito a baixo disso não será executado.
}//tudo que estiver dentro da função, só pode ser acessado chamando a função, ou dentro da função. 
console.log(soma(2, 2));


const raiz = n => n ** 0.5; //É uma função igual as outras, porem simplificada, mas funciona do mesmo jeito. 
 





// function saudacao(nome) {
//     return `Bom dia ${nome}!`;
// }
// const variavel = saudacao('Luiz');
// console.log(variavel);// retorna undefined, pois a função não está configurada para retornar nada.
